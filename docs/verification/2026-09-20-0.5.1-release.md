# 错题智库 0.5.1 发布校验

## 构建身份

- 版本：`0.5.1`
- 平台：Windows x64
- 安装器：`release/错题智库_0.5.1_x64-setup.exe`
- 大小：`7,747,533` 字节
- SHA-256：`396725E62D513B54981079257170C9D7F2BD193C978E96937B59BBE89902F7CF`
- 安装范围：当前 Windows 用户
- 代码签名：未签名

## 产品行为验证

- Windows 原生鼠标指针保持可见；页面不再注册全局 pointermove 跟踪或渲染全屏荧光跟随层。
- 导入成功后按源顺序打开第一道题，失败项仍留在待整理中。
- 完成整理会在同一事务内激活题目、移出待整理并加入当天复习；缺少题干、答案或版本冲突时保持原状态。
- 只有已激活且标准答案非空的题目进入复习队列。
- 今日复习的“稍后再看”和“结束本次”不会写入错误评分。
- 题目档案提供直接导出入口；搜索浮层支持点击背景关闭。
- 760 像素宽度下导航收为三个主要入口与“更多”，页面无横向溢出。

## 自动与视觉验证

- 前端测试：34 个文件、227 项通过，0 失败。
- Rust 测试：108 项通过，0 失败；存在两个与本次修改无关的既有 dead-code 警告。
- ESLint、TypeScript 类型检查和 Vite 生产构建均退出码 0。
- Tauri GNU x86_64 release profile 构建成功，NSIS 生成 1 个 x64 安装包。
- Chrome 实际渲染：1440×900、1024×768、760×900 均无横向溢出。
- 小屏验证：760 像素下次级导航隐藏，“更多”显示，工具栏保持可见。
- 减少动态效果媒体查询生效；交互反馈不依赖持续动画。

## 安装与首启验证

- NSIS 静默升级退出码为 0。
- 实际安装位置：`C:\Users\whz21\Desktop\错题智库`。
- 已安装主程序 ProductVersion 与 FileVersion 均为 `0.5.1`。
- `WebView2Loader.dll` 位于主程序同目录，大小 160,320 字节，SHA-256 为 `8427B1FC58EC707813E5C0A51EB5D69397BB333250A7B891BE4D3B123F1E0F1C`。
- 启动后窗口标题为“错题智库”，进程有主窗口句柄且响应正常。

## 发布边界

- 安装包未购买商业代码签名，SmartScreen 可能要求用户确认来源。
- 当前未执行卸载测试，以保留用户已经升级完成的可用安装；不将未执行项写成已验证。
- 当前不内置 OCR，也不包含自动在线更新。
