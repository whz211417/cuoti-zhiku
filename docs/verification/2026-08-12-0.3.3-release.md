# 错题智库 0.3.3 发布校验

## 构建身份

- 版本：`0.3.3`
- 平台：Windows x64
- 安装器：`release/错题智库_0.3.3_x64-setup.exe`
- 大小：`7,727,201` 字节
- SHA-256：`CA26577494449D8D0A1B1CBCBA8DA0E24CCDBE1272144F1E1927640AF03093A5`
- 安装范围：当前 Windows 用户
- 代码签名：未签名

## AI 整理与响应式验证

- AI 打开后先显示“本次只发送”摘要；题图与教材默认不发送，并收进“添加依据”。
- 无可用平台或 Key 时显示“需要先连接 AI”，不会自动跳转设置。
- “采纳全部空白字段”只处理空字段；已有内容显示“替换”，需要逐项明确操作。
- 批量采纳使用每次保存返回的新版本；模拟第二次保存失败时，首项保留且剩余建议仍可操作。
- 模拟首项保存版本冲突时，会重新载入最新文档版本并保留建议；再次采纳使用新版本写入。
- 保存期间关闭按钮不可用且 Escape 不会中断写入；最后一项处理后审核器自动收起、恢复触发按钮焦点并报告写入数量。
- 批量写入的后续字段发生外部版本冲突时同样刷新到最新版本，剩余建议可直接重试。
- 640×720 实际视觉复查：顶部课程栏由约 236 px 回落到紧凑高度，工具栏与主内容完整上移。
- 375×667、760×520、1024×600、1366×768 的布局约束由响应式测试与动态视口规则覆盖：一个垂直滚动 owner、无嵌套结果滚动、小屏操作自动堆叠、关闭按钮保持可达。
- 真实小屏设置弹层复查：标题栏固定，右侧滚动条可滚至末尾，无页面级横向溢出。
- 减少动态效果时关闭 AI 状态动画与旋转；减少透明度时使用实色纸面 fallback。

## 自动验证

- 前端测试：33 个文件、207 项测试通过，0 失败。
- Rust 测试：101 项测试通过，0 失败（GNU/MinGW 工具链）。
- ESLint：0 错误、0 警告。
- TypeScript 类型检查：退出码 0。
- Vite 生产构建：1632 个模块完成，退出码 0。
- Tauri GNU x86_64 release profile 构建成功。
- NSIS 成功生成 1 个 x64 安装包。

## 安装与首启验证

- NSIS 静默覆盖安装退出码为 0，注册版本为 `0.3.3`。
- 实际安装位置：`C:\Users\whz21\Desktop\错题智库`。
- 已安装主程序 ProductVersion 与 FileVersion 均为 `0.3.3`。
- `WebView2Loader.dll` 位于主程序同目录，大小 160,320 字节，SHA-256 为 `8427B1FC58EC707813E5C0A51EB5D69397BB333250A7B891BE4D3B123F1E0F1C`。
- 桌面快捷方式指向 0.3.3 的实际安装目录。
- 启动后窗口标题为“错题智库”，进程有主窗口句柄且响应正常。

## 发布边界

- 安装包未购买商业代码签名，SmartScreen 可能要求用户确认来源。
- 当前不内置 OCR；扫描 PDF 需要先 OCR 或手动补充题干。
- 不包含自动在线更新。
